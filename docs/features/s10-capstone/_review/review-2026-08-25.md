# Stage 10 review — 2026-08-25

Two independent reviewers in clean context, in parallel. The first went **specification → code**
(looking for what is missing), the second **code → consequences** (looking for what breaks at the
edge).

## Scope

```
faa95f6  Assemble nine stages into one service and measure the assembly
```

Modules: `seams.py`, `assemble.py`, `service.py`, `scenarios.py`, `arch.py`, `run.py`, `check.py`.
Documents: `spec.md` (10 US, 19 AC), `sad.md`, eight ADRs, `ARCHITECTURE.md`, the lesson, the
exercises.

## Summary

**14 MAJOR, 21 MINOR** (with overlaps). Every MAJOR is closed; every MINOR is closed except one
deliberately deferred (below).

The hardest were four findings in which **the stage's own thesis worked against it**.

| # | Finding | How it was closed |
|---|---|---|
| M1 | Stage 9 "worked" through a single line, and that line was the instrument itself (`sys.settrace(previous)` in the counter's `finally`) | Moved into `NOT_WIRED` with a reason; six parts remain. A run over empty work now yields zeros |
| M2 | A retrieved document travelled into the prompt raw, without stage 2's data-block fence | `from_search` hands over to stage 2's `build_prompt`; a check asserts the fence and a non-empty block |
| M3 | The price of the adapters was counted statically, the executed stage lines dynamically | Both numbers are executed lines now; `written` stayed separate, and the difference is named in the lesson |
| M4 | `_is_guard` exempted any test by name; `ast.IfExp` was not collected at all | The exception was narrowed to `if not <name>:` with a single `return`; `IfExp` and mirrored probes added |
| M5 | The parser silently dropped an unparsed row together with the dangling citation inside it | `unparsed()` makes an unparsed row a defect; three forms of probe in the check |
| M6 | `dangling()` did not reconcile the wrapping table — substituting `s99` for `s06` there passed green | One parse for both lists |
| M7 | `_section` matched `### Heading` as a substring of `## Heading` | Whole-line lookup; a probe in the check |
| M8 | One request landed both in "success" and in "refusal" | `request(OK)` at the very end; the check asserts both halves |
| M9 | The budget guard had no witness — the charge could be removed and everything stayed green | A check with the limit exhausted and a mirrored half through a substituted `charge` |
| M10 | AC-09 compared 1 with 1; the three verdict levels were never called anywhere | Grouping by request (`by_ref`), a call to `evaluate()` and a check on the order of the levels |
| M11 | AC-08: p50/p95 were printed nowhere, and `p95 >= p50` is an identity on a sorted list | `latency.py`, scene 8 of the demo, a check on the printed numbers and on the order "conditions before the number" |
| M12 | ADR-0006 promised a scenario that breaks a part; the `breaks` field was dead | A sixth scenario, with a shared failing client and a check |
| M13 | AC-01 promised "the tools used", which were in neither the answer nor the trace: the fake never called a tool | `Reply.tools`, a scenario with `get_weather`, tool reconciliation in `compare()` |
| M14 | Four documents promised a "second deploy"; no ASGI surface existed, and no AC asked about one | `serve.py` takes stage 6's `create_app`; US-11, AC-13/13b/13c; a section in `RUNBOOK.md` |
| M15 | NFR-3 (the lesson ≤ 2500 words) had no check at all | Added; and with it a reconciliation of the lesson's numbers against the measurement |
| M16 | `branch` said "search" when retrieval found nothing and the agent answered | The branch is decided by what actually fired |

MINORs closed: a duplicated definition of "executable line", a duplicated failing client, `_folder()`
inside an inner loop, a solution subprocess with no time ceiling and a brittle parse, an empty
`NOT_WIRED`, a reference to NFR-8 in the warm-up check, `startswith("s")` instead of the stage's
existence in the seams, a disagreement about "how many stages are not wired in" across three
documents, `Container(s04)` and `Rel(scenarios, s08)` in the §5 diagram, statuses in the root
READMEs.

## Deferred

- **Moving the adapters into `shared/`**, should anyone else need them. An adapter exists for a
  particular seam; moving it would make it a part with no stage. Owner: Contributor,
  due: after the course.

## What it gave in numbers

|  | Before review | After |
|---|---|---|
| Checks / on failure modes | 24 / 11 | 32 / 16 |
| Mutations | 9 | 12 |
| Parts that execute | 7 (one of them fictitious) | 6 |
| Price of assembly | 19 static / 166 | 12 executed / 173 |
| US / AC | 10 / 19 | 11 / 25 |

## Verdict

**PASS.** Every MAJOR is closed and the gates are green: `check_all` (11 modules), `clean_install`,
`docs_check`, `mutate s10 --expect`, `ruff`.
