# Рев'ю етапу 10 — 2026-08-25

Два незалежні рецензенти в чистому контексті, паралельно. Перший вів **специфікація → код**
(шукав, чого немає), другий — **код → наслідки** (шукав, що зламається на краю).

## Обсяг

```
faa95f6  Assemble nine stages into one service and measure the assembly
```

Модулі: `seams.py`, `assemble.py`, `service.py`, `scenarios.py`, `arch.py`, `run.py`, `check.py`.
Документи: `spec.md` (10 US, 19 AC), `sad.md`, вісім ADR, `ARCHITECTURE.md`, урок, вправи.

## Підсумок

**14 MAJOR, 21 MINOR** (з перетинами). Усі MAJOR закрито; MINOR закрито всі, крім одного
свідомо відкладеного (нижче).

Найважче — чотири знахідки, у яких **теза етапу спрацювала проти нього самого**.

| # | Знахідка | Як закрито |
|---|---|---|
| M1 | Етап 9 «працював» одним рядком, і той рядок — сам прилад (`sys.settrace(previous)` у `finally` лічильника) | Переїхав у `NOT_WIRED` із причиною; частин шість. Прогін на порожній роботі тепер дає нулі |
| M2 | Знайдений документ їхав у промпт сирим, без огорожі блоку даних етапу 2 | `from_search` віддає `build_prompt` етапу 2; перевірка стверджує огорожу й непорожність блоку |
| M3 | Ціна перехідників рахувалась статично, виконані рядки етапів — динамічно | Обидва числа — виконані рядки; `written` лишився окремо, і різниця названа в уроці |
| M4 | `_is_guard` звільняв будь-який тест-ім'я; `ast.IfExp` не збирався взагалі | Виняток звужено до `if not <ім'я>:` з єдиним `return`; додано `IfExp` і дзеркальні проби |
| M5 | Розбирач мовчки викидав нерозібраний рядок разом із битим посиланням у ньому | `unparsed()` робить нерозібраний рядок вадою; три форми проб у перевірці |
| M6 | `dangling()` не звіряв таблицю обвісу — підміна `s06` на `s99` там проходила зеленою | Один розбір на два переліки |
| M7 | `_section` ловив `### Заголовок` як підрядок `## Заголовок` | Пошук рядком цілком; проба в перевірці |
| M8 | Один запит потрапляв і в «успіх», і в «відмову» | `request(OK)` у самому кінці; перевірка стверджує обидві половини |
| M9 | Бюджетний запобіжник не мав свідка — списання можна було прибрати й лишитись зеленим | Перевірка з вичерпаною межею й дзеркальною половиною через підміну `charge` |
| M10 | AC-09 порівнював 1 з 1; три рівні вердиктів не викликались ніде | Групування за запитом (`by_ref`), виклик `evaluate()` і звірка порядку рівнів |
| M11 | AC-08: p50/p95 ніде не друкувались, а `p95 >= p50` — тотожність на відсортованому списку | `latency.py`, сцена 8 демо, перевірка на надруковані числа й порядок «умови перед числом» |
| M12 | ADR-0006 обіцяв сценарій, що ламає частину; поле `breaks` було мертвим | Шостий сценарій, спільний клієнт-відмовник із перевіркою |
| M13 | AC-01 обіцяв «використані інструменти», яких не було ні у відповіді, ні в трейсі: підробка ніколи не кликала інструмента | `Reply.tools`, сценарій із `get_weather`, звірка інструментів у `compare()` |
| M14 | «Другий деплой» обіцяли чотири документи; ASGI-поверхні не існувало, і жоден AC про неї не питав | `serve.py` бере `create_app` етапу 6; US-11, AC-13/13b/13c; розділ у `RUNBOOK.md` |
| M15 | NFR-3 (урок ≤ 2500 слів) не мав жодної перевірки | Додано; заодно звірка чисел уроку з виміром |
| M16 | `branch` казав «search», коли пошук нічого не знайшов і відповідав агент | Гілка визначається за тим, що спрацювало |

MINOR закрито: копію визначення «виконуваного рядка», копію клієнта-відмовника, `_folder()` у
внутрішньому циклі, підпроцес розв'язку без стелі часу й із крихким розбором, порожній
`NOT_WIRED`, посилання на NFR-8 у перевірці прогріву, `startswith("s")` замість існування етапу
у швах, розбіжність «скільки етапів не ввімкнено» між трьома документами, `Container(s04)` і
`Rel(scenarios, s08)` у діаграмі §5, статуси в кореневих README.

## Відкладене

- **Винесення перехідників у `shared/`**, якщо вони знадобляться комусь іще. Перехідник існує
  заради конкретного шва; винесення зробило б його частиною без етапу. Owner: Contributor,
  due: після курсу.

## Що це дало числами

|  | До рев'ю | Після |
|---|---|---|
| Перевірок / на режими відмови | 24 / 11 | 32 / 16 |
| Мутацій | 9 | 12 |
| Частин, що виконуються | 7 (одна фіктивна) | 6 |
| Ціна складання | 19 статичних / 166 | 12 виконаних / 173 |
| US / AC | 10 / 19 | 11 / 25 |

## Вердикт

**PASS.** Усі MAJOR закриті, ворота зелені: `check_all` (11 модулів), `clean_install`,
`docs_check`, `mutate s10 --expect`, `ruff`.
