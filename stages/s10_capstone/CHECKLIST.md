# Чекліст — етап 10

Три рівні. Пройти означає закрити всі три, а не перший.

## Я зрозумів

- [ ] Можу пояснити різницю між «етап імпортовано» і «етап працює» — і назвати, чим вона
      міряється.
- [ ] Знаю, чому теза «капстоун імпортує зріле з етапів 1–9» була викинута. Підказка: подивись
      на перші рядки `stages/s06_platform/app.py`.
- [ ] Можу пояснити, чому етап 9 переїхав із «частин» у «свідомо не ввімкнені», хоча давав
      ненульове число. Підказка: прогони `measure(lambda: None)`.
- [ ] Розумію, чому ціна складання — це **два** числа, і чому обидва мусять бути в одній
      одиниці. Можу сказати, що ставила в чисельник перша редакція.
- [ ] Можу пояснити, чому `build_search` дає три написані рядки й нуль виконаних — і чому це
      не похибка.
- [ ] Розумію, чому невідповідність іде в перехідник, **ніколи** в частину — і що зачіпає
      правка в частині, крім самої частини.
- [ ] Можу назвати дві форми, якими перехідник вирішує, і сказати, яка охорона дозволена.
- [ ] Знаю, чому `ARCHITECTURE.md` розбирається кодом, і чому **нерозібраний рядок** — це вада,
      а не тиша.
- [ ] Можу пояснити, чому знайдений текст їде в модель за огорожею блоку даних, і що саме
      відкривав капстоун, обходячи `build_prompt`.
- [ ] Розумію, чому другий деплой не коштував жодного перехідника — і що це каже про те, чим
      домовлені етапи 6 і 10.
- [ ] Знаю, чого цей етап **не** обіцяє: що шість сценаріїв є покриттям і що деплой перевірено.

## Я запустив

- [ ] `python -m stages.s10_capstone.run` — вісім сцен; третю прочитав уважніше за решту, бо
      саме вона показує, скільки з кожного етапу справді працює.
- [ ] `python -m stages.s10_capstone.check` — усі зелені; перевірок: 32, з них на режими
      відмови: 16. Дві лишаються `НЕ ПЕРЕВІРЕНО` — і я знаю, чому це третій стан, а не провал.
- [ ] `python scripts/mutate.py s10 --expect` — числа у вправах збігаються з прогоном.
- [ ] `uvicorn stages.s10_capstone.serve:app` — сервіс піднявся, і я знайшов у `serve.py`
      рядок, який показує, що власного HTTP-шару тут немає.
- [ ] Зробив вправу 10 і побачив, що зламана ціна виглядає точно як ціла.
- [ ] Подивився на вправи 3, 4, 10, 11 і 12 — це діри, які знайшов сам мутаційний прогін.
- [ ] Зламав рядок в `ARCHITECTURE.md` на три стовпці й побачив, що саме скаже перевірка.

## Я пояснив

- [ ] Розповів колезі, чому перелік імпортів не є доказом складання — і чим його замінити.
- [ ] Пояснив, чому «менше коду в капстоуні» без другого числа нічого не означає.
- [ ] Показав розділ «що складання виявило» і пояснив, чому порожній розділ був би
      найпідозрілішим результатом етапу.
- [ ] Назвав своїми словами, чому число затримки друкується **після** умов, а не перед ними.
- [ ] Пояснив, чому «прилад міряв сам себе» — це той самий клас вади, що «імпортує ≠ використовує».
