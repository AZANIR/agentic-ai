"""Сервер, який піднімається й мовчить. Існує рівно заради однієї перевірки.

Тайм-аут — той захист, який легко не поставити: без нього все працює, доки сервер відповідає.
А коли перестане, виклик не впаде — він **зависне**. Жодного винятку, жодного логу, просто
процес, який стоїть, і задача, яка ніколи не завершиться.

Щоб перевірити, що тайм-аут справді є, потрібен сервер, який справді мовчить. Мок тут не
годиться: він мовчить у тому самому процесі, і зависання виглядало б інакше.
"""

from __future__ import annotations

import sys
import time

if __name__ == "__main__":
    # Не пишемо у stdout нічого: клієнт чекає на відповідь протоколу й не дочекається.
    sys.stderr.write("mute: піднявся й мовчу\n")
    sys.stderr.flush()
    while True:
        time.sleep(3600)
