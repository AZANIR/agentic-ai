# Вправи етапу 7 — зламай і подивись, що почервоніє

Перед кожною вправою прогони набір і переконайся, що він зелений:

```bash
python -m stages.s07_voice.check
```

Числа заміряні, не вгадані; звірка автоматична:

```bash
python scripts/mutate.py s07 --expect
```

**Читай не кількість, а назви.** Мутація, спіймана випадковою перевіркою, — це гірше, ніж
спіймана тією, яка про неї стверджує.

**Три найважливіші стосуються не звуку, а вимірювання** — вправи 1, 2 і 3. Усі три
лишають код працездатним і дають числа, які виглядають правдоподібно.

---

## Вправа 1 · Підроблений годинник починає спати насправді

`stages/s07_voice/clock.py`:

```python
# було
        self.waits.append(millis)
        self._now += millis

# стало
        import time as _t

        self.waits.append(millis)
        _t.sleep(millis / 100000.0)
        self._now += millis
```

**Червоних: 1.**

Підроблений годинник починає спати насправді — зовсім трохи, у сто разів менше за
заявлене. Числа лишаються **правильними**; змінюється лише те, що прогін тепер коштує
реального часу.

На двадцяти прогонах це ще непомітно. На двохстах — перевірка починає займати секунди,
і хтось зменшить кількість прогонів. Далі мигтіння повертається, і його вимикають.

Червоніє перевірка, що міряє **справжню** тривалість підробленого сну.

---

## Вправа 2 · Перший звук можна позначити двічі

`stages/s07_voice/measure.py`:

```python
# було
        if self.timing.first_audio is not None:

# стало
        if False:
```

**Червоних: 1.**

Захист від повторного позначення прибрано. Другий фрагмент затирає перший, і
time-to-first-audio мовчки стає часом **другого** звуку.

Число лишається правдоподібним — воно просто вимірює не те. Це найгірша форма
поломки вимірювання: графік виглядає нормально.

---

## Вправа 3 · Кроки секундоміра перекриваються

`stages/s07_voice/measure.py`:

```python
# було
        cost = now - self._mark
        self._mark = now

# стало
        cost = now - self._started
```

**Червоних: 2.**

Кроки секундоміра починають перекриватися: кожен міряється від початку прогону, а не
від кінця попереднього.

Наслідок — сума кроків більша за загальний час. Саме тому перевірка стверджує
**рівність суми**, а не просто наявність чисел: розклад, що не сходиться, гірший за
його відсутність, бо йому вірять.

---

## Вправа 4 · Розпізнавання перестає перекриватися з мовленням

`stages/s07_voice/stt.py`:

```python
# було
        cost = FINALISE_MILLIS if self.incremental else self.millis_per_second * seconds

# стало
        cost = self.millis_per_second * seconds
```

**Червоних: 2.**

Розпізнавання в стрімінгу перестає перекриватися з мовленням і знову коштує повну
ціну.

Відношення падає з 3.5x до 1.6x — тобто обіцянка «щонайменше вдвічі» ламається. Це
показує, **звідки насправді** береться більша частина виграшу: не з фрагментів, а з
того, що робота переїхала в час, коли людина ще говорила.

---

## Вправа 5 · Стрімінг чекає всіх фрагментів перед першим звуком

`stages/s07_voice/pipeline.py`:

```python
# було
            if index == 0:
                watch.first_audio()

# стало
            if index == len(CHUNKS_SENTINEL) - 1:
                watch.first_audio()
```

**Червоних: 4.**

Стрімінг позначає перший звук на **останньому** фрагменті. Тобто перестає бути
стрімінгом, лишаючись ним за структурою коду.

Найбільше червоних із дванадцяти: ця мутація ламає і відношення, і розклад, і e2e.
Що не робить її найважливішою — вправа 1 дає одну червону й гірша.

---

## Вправа 6 · Barge-in дивиться лише на рівень

`stages/s07_voice/bargein.py`:

```python
# було
    if sound.millis < min_millis:

# стало
    if False:
```

**Червоних: 3.**

Barge-in перестає дивитись на тривалість. Клацання мишею перериває відповідь.

Ця й наступна вправи разом показують, навіщо перевірок три: кожна умова має власну,
і прибрати одну мовчки неможливо.

---

## Вправа 7 · Barge-in дивиться лише на тривалість

`stages/s07_voice/bargein.py`:

```python
# було
    if sound.level < level:

# стало
    if False:
```

**Червоних: 2.**

Barge-in перестає дивитись на рівень. Тепер відповідь перериває тривалий шум
кондиціонера.

Дзеркальна пара до вправи 6.

---

## Вправа 8 · P95 інтерполюється замість брати справжній прогін

`stages/s07_voice/measure.py`:

```python
# було
        p95=ordered[rank],

# стало
        p95=sum(ordered) / len(ordered) * 1.5,
```

**Червоних: 1.**

p95 стає інтерпольованим числом замість справжнього прогону.

Воно навіть виглядає розумно — півтора середнього. Але це число, якого **ніхто не
відчув**: жоден прогін стільки не тривав. Показувати користувачеві вигадану
затримку — не те саме, що показувати найгіршу з реальних.

---

## Вправа 9 · Розподіл із нуля прогонів дає нулі

`stages/s07_voice/measure.py`:

```python
# було
    if not values:
        raise ValueError("розподіл із нуля прогонів не існує")

# стало
    if not values:
        return Distribution(runs=0, mean=0.0, p95=0.0, worst=0.0)
```

**Червоних: 1.**

Розподіл із нуля прогонів повертає нулі замість помилки.

У звіті це виглядає як **дуже швидкий сервіс**: середнє 0 мс, p95 0 мс. Мовчазний
нуль там, де немає даних, — найшвидший спосіб отримати зелений дашборд на мертвій
системі.

---

## Вправа 10 · Мовчання йде в модель

`stages/s07_voice/pipeline.py`:

```python
# було
    if heard.silent:
        return _silence(watch)

# стало
    if False:
        return _silence(watch)
```

**Червоних: 1.**

Порожнє розпізнавання йде далі в модель і синтез. Кожен кашель у мікрофон коштує
токенів і півтори секунди роботи.

Перевірка ловить це не за іменами кроків, а за **затратами**: підроблений годинник
показує, що модель таки спала свої 750 мс.

---

## Вправа 11 · Prefetch перестає називати марну роботу

`stages/s07_voice/prefetch.py`:

```python
# було
        outcome.note = f"{WASTED}: інструмент викликано, результат відкинуто"

# стало
        outcome.note = ""
```

**Червоних: 2.**

Prefetch перестає називати марну роботу. Виклик так само відбувається й так само
відкидається — просто про це ніхто не дізнається.

Етап перетворюється на агітацію за prefetch: показано виграш, приховано ціну.

---

## Вправа 12 · Prefetch чекає обидві затримки замість перекриття

`stages/s07_voice/prefetch.py`:

```python
# було
    clock.sleep(max(tool_millis, think_millis))

# стало
    clock.sleep(tool_millis + think_millis)
```

**Червоних: 1.**

Prefetch починає чекати обидві затримки послідовно замість перекриття.

Він і далі «працює»: виклик відбувається раніше, код виглядає так само. Просто
виграшу більше немає — складність додано за нуль.

---

## Що робити далі

Спробуй **свою** мутацію: зламай щось і подивись, чи хтось помітить. Якщо набір лишився
зеленим — ти знайшов дірку в перевірках, і це цінніше за будь-яку з дванадцяти вище.

І окремо — покрути **пороги**:

```bash
python -c "from stages.s07_voice.bargein import *; print(should_interrupt(Sound(0.4, 250)))"
```

Знайди пару чисел, за якої твоє власне «угу» посеред відповіді її не перериває, а «стоп» —
перериває. Це і є справжня робота з VAD, і вона не автоматизується.