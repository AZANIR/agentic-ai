# secrets
.env
.env.*
!.env.example
!.env.prod.example
deploy/secrets/

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# data / artifacts
*.db
*.sqlite3
traces/
data/vectors/
models/
*.log

# os / editors
.DS_Store
Thumbs.db
.idea/
.vscode/

# Per-developer SDD settings
.claude/*.local.md
.worktrees/

# Article drafts, if any are ever kept here again. The published articles live in the
# blog repository; this repository links to them from docs/readme.md.
sources/

# IDE specifics; the plan itself is kept in planning/
.cursor/

# Секрети продакшн-збірки живуть на машині, не в репозиторії.
deploy/.env.prod

# uv is not used by this project: pip plus pyproject. `uv run` leaves this behind.
uv.lock
